{
  "$schema": "vendor/phpbench/phpbench/phpbench.schema.json",
  "runner.bootstrap": "vendor/autoload.php",
  "runner.path": "src",
  "runner.php_config": {
    "memory_limit": "512M"
  },
  "runner.revs": 10,
  "runner.iterations": 5,
  "report.generators": {
    "report_plot": {
      "title": "inwebo/csv-reader vs league/csv",
      "description": "Performance comparison — 4 scenarios × 3 file sizes",
      "generator": "component",
      "components": [
        {
          "component": "bar_chart_aggregate",
          "title": "Execution time by scenario (ms)",
          "x_partition": "subject_name",
          "bar_partition": "benchmark_name",
          "y_expr": "mode(partition['result_time_avg']) as time",
          "y_axes_label": "yValue as time"
        },
        {
          "component": "bar_chart_aggregate",
          "title": "Memory consumption by scenario",
          "x_partition": "subject_name",
          "bar_partition": "benchmark_name",
          "y_expr": "mode(partition['result_mem_peak']) as memory",
          "y_axes_label": "yValue as memory"
        },
        {
          "component": "section",
          "tabbed": true,
          "partition": [
            "subject_name"
          ],
          "tab_labels": ["Read CSV file", "Filter CSV File", "Normalize CSV File", "Read large CSV file (6.1 Mo)"],
          "components": [
            {
              "title": "{{ first(frame['subject_name']) }}",
              "component": "section",
              "components": [
                {
                  "component": "bar_chart_aggregate",
                  "title": "Time Comparison (ms)",
                  "x_partition": "variant_name",
                  "bar_partition": "benchmark_name",
                  "y_expr": "mode(partition['result_time_avg']) as time",
                  "y_axes_label": "yValue as time"
                },
                {
                  "component": "bar_chart_aggregate",
                  "title": "Memory Comparison (bytes)",
                  "x_partition": "variant_name",
                  "bar_partition": "benchmark_name",
                  "y_expr": "mode(partition['result_mem_peak']) as memory",
                  "y_axes_label": "yValue as memory"
                },
                {
                  "title": "Detailed stats",
                  "component": "table_aggregate",
                  "partition": [
                    "benchmark_name",
                    "variant_name"
                  ],
                  "row": {
                    "Library": "if(first(partition['benchmark_name']) = \"InweboCsvReaderBenchmark\", 'inwebo/csv-reader', 'league/csv')",
                    "Variant": "first(partition['variant_name'])",
                    "Peak memory": "first(partition['result_mem_peak']) as memory",
                    "Min time": "min(partition['result_time_avg']) as time",
                    "Average time": "mode(partition['result_time_avg']) as time",
                    "Max time": "max(partition['result_time_avg']) as time",
                    "Relative standard deviation": "rstdev(partition['result_time_avg']) "
                  }
                },
                {
                  "title": "Variant Comparison",
                  "component": "section",
                  "partition": [
                    "variant_name"
                  ],
                  "components": [
                    {
                      "title": "Variant Comparison (Time %)",
                      "component": "table_aggregate",
                      "partition": [
                        "variant_name"
                      ],
                      "row": {
                        "Library": "if(first(partition['benchmark_name']) = \"InweboCsvReaderBenchmark\", 'inwebo/csv-reader', 'league/csv')",
                        "Variant": "first(partition['variant_name'])",
                        "Difference": "percent_diff(max(frame['result_time_avg']), min(frame['result_time_avg']))"
                      }
                    },
                    {
                      "title": "Memory % ({{ first(frame['variant_name']) }})",
                      "component": "table_aggregate",
                      "partition": [
                        "variant_name"
                      ],
                      "row": {
                        "Library": "if(first(partition['benchmark_name']) = \"InweboCsvReaderBenchmark\", 'inwebo/csv-reader', 'league/csv')",
                        "Variant": "first(frame['variant_name'])",
                        "Difference": "percent_diff(max(partition['result_mem_peak']), first(frame['result_mem_peak']))"
                      }
                    }
                  ]
                }
              ]
            }
          ]
        }
      ]
    }
  }
}
